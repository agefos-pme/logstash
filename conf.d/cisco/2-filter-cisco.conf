filter {

  if [type] == "cisco-netflow9-log" and [host] =~ "10\.100\.1\.223" {

    mutate {
      update => { "type" => "netflow9-log" }
      add_field => { "netflow9_real_timestamp" => "%{@timestamp}" }
    }
  }
}
